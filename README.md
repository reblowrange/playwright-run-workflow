# Getting Started with Create React App

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## Steps to create your own tests

1. Run the ```npm install``` command on the root of the project.
2. Start the web app by running the ```npm start``` command.
3. Start the test using ```npm run safetest```.  